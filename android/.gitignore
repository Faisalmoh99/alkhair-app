gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
# Exception: the shared debug-only signing keystore is intentionally tracked
# (universal default debug credentials, not a real secret) — see root .gitignore.
!debug.keystore
