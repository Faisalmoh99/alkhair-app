# شرح فيديو العرض التوضيحي — تطبيق الخير (Al-Khair)

**الملف المصدر:** `alkhair.mov` (المدة: 5:20 دقيقة، محاكي iPhone 16e / iOS 26.2)
**طريقة الاستخراج:** تحليل الفيديو بالكشف عن تغيّر المشاهد (scene-change detection) بالإضافة إلى إطار كل 3 ثوانٍ كشبكة أمان، لضمان عدم تفويت أي انتقال.

---

## الجزء الأول — ماذا حدث في الفيديو

هذا سرد زمني لكل ما يظهر في الفيديو (بدون صوت)، بالترتيب الذي يظهر فيه، مع الطابع الزمني التقريبي لكل خطوة. الفيديو يوثّق اختبارًا يدويًا شاملاً يغطي: تسجيل متطوّع جديد، اعتماد المسؤول له، تسجيل متبرّع جديد وإنشاء بلاغ فائض غذائي، قبول المتطوّع للبلاغ وتسليمه، ثم رجوع مسؤول الجمعية للتحقق من تحديث اللوحة.

### 1. فتح التطبيق (00:00 – 00:33)
- **00:00–00:24** — الشاشة الرئيسية لمحاكي iPhone، يقوم المستخدم بالنقر على أيقونة تطبيق "Al-Khair App".
- **00:25–00:33** — تظهر شاشة البداية (Splash Screen): شعار التطبيق (مربع كُحلي بورقة خضراء ووعاء ذهبي)، عنوان "الخير"، وشعار فرعي "منصة توزيع الفائض الغذائي".

### 2. تسجيل حساب متطوّع جديد (00:34 – 01:35)
- **00:34** — تظهر شاشة "تسجيل الدخول": حقل لإدخال رقم الجوال، وزر "إرسال رمز التحقق".
- **00:34–00:55** — يُدخل المستخدم الرقم **0500000000** ويضغط الإرسال. تفتح نافذة داخل التطبيق تُحمّل من نطاق Firebase الحقيقي (`...bisha-dev.firebaseapp.com`) وتعرض رسالة "Verifying you're not a robot..." — هذا تحدي reCAPTCHA الحقيقي الخاص بخدمة Firebase Phone Authentication (وليس بيئة المحاكي/Emulator)، ما يؤكد أن هذا الاختبار تم على مشروع Firebase فعلي.
- **~01:00–01:03** — تظهر شاشة "رمز التحقق" وتوضح أن الرمز أُرسل إلى `+966500000000+`. يُدخل المستخدم رمزًا من 6 خانات ويضغط "تحقق".
- **~01:10–01:20** — تظهر شاشة "أكمل بياناتك" (نوع الحساب): يكتب المستخدم الاسم **"Omar"**، ثم يختار نوع الحساب **"متطوّع"** (وليس متبرّع).
- **~01:27–01:35** — تظهر شاشة "بيانات المتطوّع": يُدخل بريدًا إلكترونيًا (`omar@gmail.com`)، نوع المركبة (`Kia`)، ويختار الجمعية من قائمة منسدلة، ثم يضغط "إرسال الطلب".
- **~01:35** — تظهر شاشة "طلبك قيد المراجعة" (أيقونة ساعة رملية) توضح أن الحساب بانتظار اعتماد الجمعية له.

### 3. تسجيل دخول مسؤول الجمعية واعتماد المتطوّع (01:46 – 02:16)
- **~01:46–02:00** — تسجيل دخول جديد برقم **0500000099**، مع شاشة تحقق برمز مرسل إلى `+966500000099`.
- **~02:00** — يدخل المستخدم إلى شاشة **"اعتماد المتطوعين"** — تظهر بطاقة واحدة للمتطوّع "Omar" (الرقم `966500000000+`، المركبة `Kia`) مع زرَي قبول (✓ أخضر) ورفض (✕ أحمر).
- المستخدم يضغط على العلامة الخضراء لاعتماد Omar كمتطوّع.

### 4. تسجيل متبرّع جديد وإنشاء بلاغ فائض غذائي (02:16 – 03:24)
- **~02:16–02:28** — يسجّل مسؤول الجمعية الخروج (انتقال يعرض شاشة تسجيل الدخول مرة أخرى). تظهر أثناء محاولات إدخال أرقام مختلفة نافذة تحدي reCAPTCHA بالصور تطلب اختيار "جميع الصور التي بها سيارات" — تأكيد إضافي على أن التحقق يمر عبر خدمة reCAPTCHA الحقيقية.
- **~02:33–02:45** — يُدخل رقم جديد **0500000003**، ويتم التحقق من الرمز المرسل إلى `+966500000003`.
- **~02:45–02:56** — تظهر شاشة "أكمل بياناتك" من جديد لهذا الحساب الجديد، وبعد إتمامها تفتح شاشة **"المتبرّع"** مباشرة (دون الحاجة لموافقة الجمعية، بعكس المتطوّع) — وتضم زرَّين: "إبلاغ عن فائض غذائي" و"حالة البلاغات والإشعارات".
- **~02:58–03:24** — يضغط المستخدم "إبلاغ عن فائض غذائي" فتظهر شاشة تعبئة البلاغ: نوع الطعام (يختار "مخبوزات")، الكمية، تاريخ الجاهزية (الأحد 19 يوليو)، الوقت (5:33)، مربع تأكيد "أؤكد أن الطعام آمن للتوزيع والاستهلاك"، وزر "إرسال البلاغ". بعد الإرسال يمكن رؤية حالة البلاغ في شاشة "حالة البلاغات" كخطوة أولى من أربع خطوات: مُسجَّل ← مُعيَّن ← تم الاستلام ← تم التسليم.

### 5. قبول المتطوّع للبلاغ وتنفيذ التسليم (03:21 – 03:53)
- **~03:21–03:31** — دورة تسجيل دخول أخرى (على الأرجح دخول Omar كمتطوّع معتمد الآن)، مع ظهور تحدي reCAPTCHA بالصور مرة أخرى يطلب اختيار "الصور التي تحتوي على محبس إطفاء حريق".
- **~03:31–03:53** — تفتح شاشة **"التوجيه والتسليم"**: تعرض حالة البلاغ "مُعيَّن"، مسافة/وقت تقديري (0.0 كم ~ 0 دقيقة)، خريطة جوجل مدمجة، زر "بدء التوجيه"، وزرّي "تأكيد الاستلام" و"تأكيد التسليم" (الأخير معطّل إلى أن يتم الاستلام أولاً). لاحظ ظهور خريطة حقيقية بعلامات مواقع مثل "UNION SQUARE" — إحداثيات افتراضية لموقع المحاكي، وليست موقعًا فعليًا في السعودية.

### 6. اختبارات إضافية ورجوع مسؤول الجمعية (03:59 – 05:20)
- **~03:59–04:42** — عدة دورات إضافية من شاشات تسجيل الدخول وإدخال رموز التحقق — يبدو أن المستخدم كان يعيد اختبار الحسابات المختلفة (تبديل بين المتبرّع/المتطوّع/المسؤول) للتأكد من سلامة تدفق الأدوار.
- **~04:42–05:20 (نهاية الفيديو)** — تسجيل دخول أخير برقم مسؤول الجمعية (0500000099)، تفتح **"لوحة الجمعية"** وتُظهر خمس بطاقات إحصائية: مُعيَّن = 0، مُسجَّل = 0، **تم التسليم = 4** (بعد أن كانت 3 في بداية الفيديو)، تم الاستلام = 0، منتهي = 0؛ رسم بياني أسبوعي "البلاغات خلال الأسبوع" يُظهر عمودين (أمس = 3، اليوم = 1)؛ وقائمة "أحدث البلاغات" تُظهر البلاغ الجديد ("مخبوزات") بحالة **"تم التسليم"** (بطاقة خضراء) بتوقيت `04:33 2026/07/19` — وهو نفس توقيت إرسال البلاغ الذي شوهد في الخطوة 4، ما يؤكد أن السيناريو الكامل (بلاغ → تعيين → استلام → تسليم) تم تنفيذه بنجاح من طرف إلى طرف خلال الفيديو.

> **ملاحظة:** بعض اللحظات (مثل نوافذ reCAPTCHA وصفحات `firebaseapp.com`) ليست جزءًا من واجهة التطبيق نفسه، بل هي نوافذ تحقق تابعة لخدمة Firebase Authentication تظهر داخل متصفح مدمج أثناء عملية تسجيل الدخول بالجوال، ولذلك لا تقابلها شاشة أو Figure في تصميم النظام.

---

## الجزء الثاني — الشرح التقني لكل شاشة

جميع المسارات معرّفة في `lib/core/router/app_router.dart` (كائن `Routes`)، والحراسة (تحديد من يصل لأي شاشة حسب حالة الحساب) في `lib/core/router/auth_guard.dart` عبر `AuthStage`. جميع دوال الخادم (Cloud Functions) منشورة في منطقة **`europe-west1`** (`functions/src/region.ts`) — تم اختيار هذه المنطقة بدل `me-central2` الأصلية بسبب قيود تفعيل الخدمة، كما هو موثّق في الكود.

### أ) شاشة تسجيل الدخول (رقم الجوال)
- **الملف:** `lib/features/auth/presentation/screens/sign_in_screen.dart` — **المسار:** `/sign-in`
- عند الضغط على "إرسال رمز التحقق" يُستدعى `authControllerProvider.notifier.requestOtp(e164)`، الذي يستدعي `FirebaseAuthRepository.requestOtp()` (`lib/features/auth/data/repositories/firebase_auth_repository.dart`) وهذا بدوره يستدعي `FirebaseAuth.verifyPhoneNumber` مباشرة من SDK Firebase — وهو ما يفتح نافذة reCAPTCHA/الويب التي شوهدت في الفيديو. لا يوجد أي كتابة على Firestore في هذه الخطوة.

### ب) شاشة رمز التحقق (OTP)
- **الملف:** `lib/features/auth/presentation/screens/otp_screen.dart` — **المسار:** `/sign-in/otp`
- عند الضغط "تحقق" يُستدعى `authControllerProvider.notifier.confirmOtp(code)` → `FirebaseAuthRepository.confirmOtp()` الذي يبني `PhoneAuthProvider.credential` ويناديه عبر `signInWithCredential`. عند النجاح، ينتقل التوجيه (`GoRouter` + `authRedirect`) تلقائيًا حسب حالة المستخدم (`AuthStage`) المشتقة من قراءة وثيقة المستخدم في Firestore.

### ج) شاشة "أكمل بياناتك" / اختيار نوع الحساب
- **الملف:** `lib/features/auth/presentation/screens/account_type_screen.dart` — **المسار:** `/sign-in/account-type`
- عند اختيار الدور، تُستدعى `createUserProfile()` في `FirebaseAuthRepository` والتي تكتب وثيقة جديدة في مجموعة **`Users/{uid}`** بالحقول: `user_id`, `name`, `phone`, `role`, `created_at`. دالة الخادم `onUsersWrite` (`functions/src/on_users_write.ts`, مُشغَّلة عند أي كتابة على `Users/{uid}`، المنطقة `europe-west1`) تقرأ الدور المكتوب وتُزامن Custom Claims على حساب المصادقة (Firebase Auth) تلقائيًا — وهذا ما يجعل التوجيه لاحقًا (متبرّع/متطوّع/مسؤول) يعمل دون إعادة تسجيل دخول.

### د) شاشة بيانات المتطوّع + شاشة "قيد المراجعة"
- **الملف:** `lib/features/auth/presentation/screens/volunteer_extra_step_screen.dart` — **المسار:** `/sign-in/volunteer-details`
- عند "إرسال الطلب" تُستدعى `completeVolunteerRegistration()` التي تنفّذ عملية Batch على Firestore: تحديث `Users/{uid}` بالبريد الإلكتروني، وإنشاء وثيقة جديدة في **`Volunteers/{uid}`** بالحقول `user_id`, `charity_id`, `approval_status: pending`, `vehicle_type`, `current_lat/lng`. الكتابة على `Volunteers/{uid}` تُشغّل أيضًا `onVolunteerWrite` (نفس ملف `on_users_write.ts`) والذي يُزامن `approval_status` و`charity_id` ضمن الـ Claims. شاشة "قيد المراجعة" هي حالة عرض ضمن نفس الملف (`_StatusView`) تُعرض تلقائيًا طالما `approval_status != approved`.

### هـ) شاشة اعتماد المتطوعين (مسؤول الجمعية)
- **الملف:** `lib/features/charity_admin/presentation/screens/volunteer_approval_screen.dart` — **المسار:** `/admin/volunteers`
- هذه هي **الشاشة 7** في تصميم النظام (Fig 5.7 / FR10). عند الضغط على علامة القبول، يُستدعى `approvalActionControllerProvider.notifier.setApproval(uid, ApprovalStatus.approved)` والذي يكتب مباشرة (وليس عبر Cloud Function) تحديث الحقل `approval_status` في وثيقة `Volunteers/{uid}` إلى `approved`. هذه الكتابة المباشرة مسموح بها بموجب قواعد الأمان لأنها من حساب مسؤول جمعية موثّق، وتُشغّل بدورها `onVolunteerWrite` لتحديث الصلاحيات (Claims) فورًا.

### و) لوحة المتبرّع الرئيسية
- **الملف:** `lib/features/donor/presentation/screens/donor_home_screen.dart` — **المسار:** `/donor/home`
- شاشة تنقّل بسيطة (لا قراءة/كتابة بيانات) تُوجّه إلى `/donor/report` (الشاشة 2) أو `/donor/status` (الشاشة 3)، وزر تسجيل الخروج يستدعي `authRepositoryProvider.signOut()`.

### ز) شاشة إنشاء بلاغ الفائض الغذائي
- **الملف:** `lib/features/donor/presentation/screens/donor_report_screen.dart` — **المسار:** `/donor/report`
- عند الضغط على "إرسال البلاغ" (مفعّل فقط بعد تفعيل مربع تأكيد السلامة) يُستدعى `reportControllerProvider.notifier.submit(...)`. **البلاغ لا يُكتب مباشرة على Firestore من التطبيق** — بل يُستدعى عبر دالة سحابية قابلة للاستدعاء (Callable Function) هي **`createDonationReport`** (`functions/src/create_donation_report.ts`, منطقة `europe-west1`). هذا التصميم مقصود: قواعد الأمان (`firestore.rules`) تمنع أي إنشاء مباشر لوثيقة في `DonationReports` من العميل، لمنع تجاوز حد معدّل الإرسال (Rate Limit — 20 بلاغًا/ساعة لكل متبرّع) الذي تفرضه الدالة عبر معاملة (Transaction) تتحقق من عدّاد `RateLimits/report_{uid}` قبل كتابة وثيقة جديدة في `DonationReports` بالحقول: `report_id`, `donor_id`, `volunteer_id: null`, `food_category`, `quantity`, `readiness_time`, `latitude/longitude`, `safety_confirmed`, `status: reported`. فور إنشاء الوثيقة، تُشغَّل دالة أخرى تلقائيًا: **`onDonationReportCreated`** (نفس المنطقة) التي تبحث عن المتطوعين المعتمدين ضمن نصف قطر 10 كم (بصيغة Haversine) وتكتب لهم وثائق تنبيه في مجموعة `Notifications`.

### ح) شاشة حالة البلاغات (المتبرّع)
- **الملف:** `lib/features/donor/presentation/screens/donor_status_screen.dart` — **المسار:** `/donor/status`
- هذه هي **الشاشة 3** (Fig 5.3 / FR5). تعرض متتبعًا أفقيًا من 4 خطوات (`_StatusTracker`) مبنيًا على قراءة حيّة (Stream) لأحدث بلاغ للمتبرّع من `DonationReports`، وتُترجم قيمة الحقل `status` (`reported` → `assigned` → `collected` → `delivered`) إلى تسميات عربية عبر الدالة `statusLabel()`.

### ط) شاشة التوجيه والتسليم (المتطوّع)
- **الملف:** `lib/features/volunteer/presentation/screens/volunteer_nav_screen.dart` — **المسار:** `/volunteer/navigate` (تُفتح بتمرير `reportId` كـ `extra`)
- هذه هي **الشاشة 5** (Fig 5.5 / FR8-FR9). زر "بدء التوجيه" لا يستخدم Directions API داخليًا؛ بل يبني رابط خرائط جوجل قياسيًا (`https://www.google.com/maps/dir/?api=1&destination=...&travelmode=driving`) ويفتحه بتطبيق الخرائط الخارجي عبر حزمة `url_launcher` — تسليم فعلي للملاحة التفصيلية بدل إعادة بنائها داخل التطبيق. زر "تأكيد الاستلام" مفعّل فقط عندما تكون حالة البلاغ `assigned`، ويستدعي `deliveryControllerProvider.notifier.confirmCollection(reportId)` → `FirebaseDonationRepository.confirmCollection()` الذي يحدّث الحقل `status` مباشرة إلى `collected` عبر معاملة (Transaction) على Firestore. بالمثل، "تأكيد التسليم" (مفعّل فقط عند `collected`) يستدعي `confirmDelivery(reportId)` الذي يحدّث `status` إلى `delivered`. كلا الإجراءين كتابة مباشرة من العميل (وليس عبر Cloud Function)، ضمن معاملة تمنع تعارض التحديثات المتزامنة.
- ملاحظة: قبول البلاغ الأولي من طرف المتطوّع (تحويل الحالة من `reported` إلى `assigned` وربط `volunteer_id`) يتم عبر `FirebaseDonationRepository.acceptReport()` بنفس أسلوب المعاملة المباشرة — وهذا يحدث في شاشة التنبيهات (Screen 4, `volunteer_alert_screen.dart`) قبل الوصول لشاشة التوجيه والتسليم.

### ي) لوحة الجمعية (مسؤول الجمعية)
- **الملف:** `lib/features/charity_admin/presentation/screens/admin_dashboard_screen.dart` — **المسار:** `/admin/dashboard`
- هذه هي **الشاشة 6** (Fig 5.6 / FR11). جميع البطاقات، الرسم البياني الأسبوعي، وقائمة "أحدث البلاغات" مبنية على قراءة حيّة (Stream) لمجموعة `DonationReports` بالكامل عبر `dashboardStatsProvider`، الذي يجمّع (aggregate) العدّادات وبيانات الرسم البياني على جهاز العميل مباشرة (بدون دالة خادم مخصصة لهذه الشاشة) — لذلك ظهر تحديث "تم التسليم" من 3 إلى 4 فور اكتمال دورة البلاغ الجديد، لأن الشاشة تستمع للتغييرات لحظيًا وليس عبر إعادة تحميل يدوي.

---

## مطابقة الجزء الأول مع الجزء الثاني

تمت مراجعة تسلسل الشاشات في الجزء الأول مقابل الشاشات الموثّقة في الجزء الثاني للتأكد من التطابق: كل شاشة ظهرت في السرد الزمني (تسجيل الدخول، الرمز، أكمل بياناتك، بيانات المتطوّع، قيد المراجعة، اعتماد المتطوعين، لوحة المتبرّع، إبلاغ عن فائض غذائي، حالة البلاغات، التوجيه والتسليم، لوحة الجمعية) لها مقابل تقني موثّق بنفس الترتيب، ولا يوجد تعارض بين الوصف المرئي والتنفيذ البرمجي.
